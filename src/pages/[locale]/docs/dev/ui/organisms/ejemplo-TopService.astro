---
import Layout from "@/layouts/Layout.astro";
import { getStaticLanguagePaths } from "@/i18n/utils";
import { TopService } from "@/components/organisms/TopService";

export async function getStaticPaths() {
  return getStaticLanguagePaths();
}

// Ya no necesitamos datos mock; el componente hará fetch automático de los primeros 3 servicios.
---

<Layout title="Servicios Destacados - Marketplace">
  <div class="min-h-screen bg-gray-50 py-8 lg:py-12">
    <TopService 
      client:load
      title="Servicios Destacados"
      buttonText="Ver Todos"
      onButtonClick={() => {
        console.log('Button clicked');
      }}
      className="mb-12 lg:mb-16"
    />
  </div>
</Layout>