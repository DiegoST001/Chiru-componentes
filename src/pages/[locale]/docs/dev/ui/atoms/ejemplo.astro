---
// filepath: d:\CHIRU\FRONT\frontend\frontend\src\pages\[locale]\docs\dev\ui\atoms\ejemplo.astro
import Layout from "@/layouts/Layout.astro";
import { getStaticLanguagePaths } from "@/i18n/utils";
import { Button } from "@/components/atoms/Button";
import { Pencil, Trash } from "phosphor-react";

export async function getStaticPaths() {
  return getStaticLanguagePaths();
}
---

<Layout title="Ejemplo de Botón">
  <Button
    variant="primary"
    size="large"
    positionIcon="right"
    text="Editar"
    disabled={false}
    fullWidth={false}
  >
    <Pencil slot="icon" />
  </Button>

  <Button
    variant="secondary"
    size="large"
    positionIcon="left"
    text="Editar"
    disabled={false}
    fullWidth={false}
  >
    <!-- <Pencil slot="icon" /> -->
  </Button>

  <Button
    variant="primary"
    size="large"
    positionIcon="left"
    disabled={false}
    fullWidth={false}
  >
    <Pencil slot="icon" />
  </Button>
</Layout>
